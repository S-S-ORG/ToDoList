import React from "react";
// import { useAuth } from "./context/auth-context";

const ToDoListPage = () => {
  // const user = useAuth().user;
  // const logout = useAuth().logout;

  return <h1>this is the initial // ToDoListPage</h1>;
};

export default ToDoListPage;
